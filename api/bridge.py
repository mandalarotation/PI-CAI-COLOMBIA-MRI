"""Prediction app."""


def load(path: str) -> dict:
    """Load model."""

    _ = path

    return {}


def predict(model: dict, body: dict) -> dict:
    """Predict with model."""
    _ = model
    _ = body

    return {}
